/** @type {import('next').NextConfig} */
const nextConfig = {
  // Bestehende statische Dateien (index.html, checkout.html etc.)
  // liegen jetzt in /public und werden direkt ausgeliefert.
  // Next.js App-Router übernimmt nur /app Routen.
};

module.exports = nextConfig;
